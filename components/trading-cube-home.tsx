'use client'

import { FormEvent, useEffect, useState } from 'react'
import { ArrowRight, BookOpen, Check, CircleHelp, FileUp, Mail, Menu, MessageCircle, Play, Settings, ShieldCheck, Sparkles, Video, X } from 'lucide-react'
import { createClient } from '@/lib/supabase/client'

type View = 'home' | 'login' | 'signup' | 'dashboard' | 'admin'

function BrandMark() {
  return <div className="flex items-center gap-3"><div className="relative flex size-10 items-center justify-center bg-yellow text-black"><span className="absolute left-2 top-2 size-3 rounded-sm border-2 border-black" /><span className="absolute bottom-2 right-2 size-3 rounded-sm border-2 border-black" /><span className="absolute right-2 top-2 size-3 rounded-sm bg-black" /><span className="absolute bottom-2 left-2 size-3 rounded-sm bg-black" /></div><span className="font-mono text-xs font-bold uppercase tracking-[0.24em]">The Trading Cube</span></div>
}

function HelpBlock() {
  return <section className="trading-panel flex flex-col gap-5 p-5 md:flex-row md:items-center md:justify-between md:p-6"><div className="flex gap-4"><div className="flex size-11 shrink-0 items-center justify-center rounded-full bg-yellow text-black"><CircleHelp /></div><div><p className="font-mono text-[10px] font-bold uppercase tracking-[0.2em] text-yellow">Need more help?</p><h3 className="mt-1 text-lg font-semibold">We are here to help you move forward.</h3><p className="mt-1 text-sm leading-6 text-muted-foreground">Reach the academy team whenever you need a second perspective.</p></div></div><div className="flex gap-3"><a className="trading-button-primary inline-flex items-center gap-2 text-sm" href="https://wa.me/447757464428?text=Hello%20Trading%20Cube%20Academy"><MessageCircle data-icon="inline-start" /> WhatsApp</a><a className="trading-button-secondary inline-flex items-center gap-2 text-sm" href="mailto:support@thetradingcube.com"><Mail data-icon="inline-start" /> Email us</a></div></section>
}

export default function TradingCubeHome() {
  const [view, setView] = useState<View>('home')
  const [menuOpen, setMenuOpen] = useState(false)
  const [user, setUser] = useState<any>(null)
  const [supabase, setSupabase] = useState<ReturnType<typeof createClient> | null>(null)

  useEffect(() => { const client = createClient(); setSupabase(client); client.auth.getUser().then(({ data }) => { setUser(data.user); if (data.user) setView('dashboard') }); const { data: listener } = client.auth.onAuthStateChange((_event, session) => setUser(session?.user ?? null)); return () => listener.subscription.unsubscribe() }, [])
  const go = (next: View) => { setView(next); setMenuOpen(false); window.scrollTo({ top: 0, behavior: 'smooth' }) }
  const signOut = async () => { if (supabase) await supabase.auth.signOut(); setUser(null); go('home') }

  if (view === 'login' || view === 'signup') return supabase ? <AuthView mode={view} go={go} supabase={supabase} /> : null
  if (view === 'dashboard') return <Dashboard user={user} signOut={signOut} go={go} />
  if (view === 'admin') return <AdminView go={go} />

  return <main className="min-h-screen bg-background"><header className="mx-auto flex max-w-7xl items-center justify-between px-5 py-6 lg:px-8"><BrandMark /><nav className="hidden items-center gap-8 md:flex"><a href="#academy" className="text-sm text-muted-foreground hover:text-foreground">The academy</a><a href="#method" className="text-sm text-muted-foreground hover:text-foreground">Our method</a><button onClick={() => go(user ? 'dashboard' : 'login')} className="trading-button-primary text-sm">{user ? 'Open dashboard' : 'Member login'}</button></nav><button className="md:hidden" onClick={() => setMenuOpen(!menuOpen)} aria-label="Toggle navigation">{menuOpen ? <X /> : <Menu />}</button></header>{menuOpen && <div className="mx-5 flex flex-col gap-4 border border-border bg-card p-5 md:hidden"><a href="#academy">The academy</a><a href="#method">Our method</a><button onClick={() => go(user ? 'dashboard' : 'login')} className="trading-button-primary">{user ? 'Open dashboard' : 'Member login'}</button></div>}<section className="border-y border-border"><div className="mx-auto grid max-w-7xl items-center gap-12 px-5 py-20 lg:grid-cols-[1.1fr_0.9fr] lg:px-8 lg:py-28"><div><div className="mb-7 inline-flex items-center gap-2 border border-yellow/40 bg-yellow/10 px-3 py-2 font-mono text-[10px] uppercase tracking-[0.2em] text-yellow"><Sparkles size={13} /> Created by traders, for traders</div><h1 className="max-w-3xl text-balance text-5xl font-bold leading-[0.98] tracking-tight md:text-7xl">Trade with <span className="text-yellow">clarity.</span><br />Learn with intent.</h1><p className="mt-7 max-w-xl text-pretty text-base leading-7 text-muted-foreground md:text-lg">A structured trading education built for people who want a repeatable process, not another collection of random signals.</p><div className="mt-9 flex flex-col gap-3 sm:flex-row"><button onClick={() => go(user ? 'dashboard' : 'signup')} className="trading-button-primary inline-flex items-center justify-center gap-2">{user ? 'Continue learning' : 'Create your account'} <ArrowRight data-icon="inline-end" /></button><button onClick={() => go('login')} className="trading-button-secondary">Already a member</button></div></div><div className="relative min-h-80 border border-border bg-card p-6"><div className="absolute right-6 top-6 size-28 border-8 border-yellow/80" /><div className="absolute bottom-8 left-8 size-20 border-8 border-yellow/50" /><div className="relative flex h-full min-h-64 flex-col justify-end"><p className="font-mono text-[10px] uppercase tracking-[0.2em] text-yellow">The Trading Cube Academy</p><h2 className="mt-3 max-w-sm text-3xl font-bold">A better process beats a louder opinion.</h2><div className="mt-8 flex items-center gap-3 border-t border-border pt-4 text-xs text-muted-foreground"><ShieldCheck className="text-yellow" /> Structured. Measured. Yours.</div></div></div></div></section><section id="academy" className="mx-auto max-w-7xl px-5 py-20 lg:px-8"><div className="grid gap-10 md:grid-cols-3"><Feature icon={<Video />} title="Video lessons" copy="Focused lessons hosted on YouTube and organised into a clear path." /><Feature icon={<BookOpen />} title="Written education" copy="Keep the concepts close with concise reading material and practical notes." /><Feature icon={<Check />} title="Knowledge checks" copy="Test what you have learned before you move to the next section." /></div></section><section id="method" className="border-y border-border bg-card"><div className="mx-auto max-w-7xl px-5 py-20 lg:px-8"><div className="max-w-2xl"><p className="font-mono text-xs uppercase tracking-[0.2em] text-yellow">Support when it matters</p><h2 className="mt-4 text-4xl font-bold tracking-tight">You do not have to figure it out alone.</h2><p className="mt-4 leading-7 text-muted-foreground">Every section gives you a direct route to the academy team through WhatsApp or email, so questions never become roadblocks.</p></div><div className="mt-10"><HelpBlock /></div></div></section><footer className="mx-auto flex max-w-7xl flex-col gap-3 px-5 py-8 text-xs text-muted-foreground sm:flex-row sm:items-center sm:justify-between lg:px-8"><BrandMark /><span>© 2026 The Trading Cube Academy</span></footer></main>
}

function Feature({ icon, title, copy }: { icon: React.ReactNode; title: string; copy: string }) { return <div className="flex flex-col gap-4"><div className="flex size-11 items-center justify-center border border-yellow text-yellow">{icon}</div><h3 className="text-xl font-semibold">{title}</h3><p className="text-sm leading-6 text-muted-foreground">{copy}</p></div> }

function AuthView({ mode, go, supabase }: { mode: 'login' | 'signup'; go: (v: View) => void; supabase: ReturnType<typeof createClient> }) { const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [name, setName] = useState(''); const [message, setMessage] = useState(''); const submit = async (event: FormEvent) => { event.preventDefault(); setMessage(''); const result = mode === 'login' ? await supabase.auth.signInWithPassword({ email, password }) : await supabase.auth.signUp({ email, password, options: { emailRedirectTo: process.env.NEXT_PUBLIC_DEV_SUPABASE_REDIRECT_URL ?? `${window.location.origin}/auth/callback`, data: { full_name: name } } }); if (result.error) setMessage(result.error.message.toLowerCase().includes('confirm') ? 'Please confirm your email before signing in.' : 'Unable to complete that request. Check your details and try again.'); else setMessage(mode === 'signup' ? 'Account created. Check your email to confirm access.' : 'Signed in successfully.') }; return <main className="min-h-screen bg-background"><header className="mx-auto flex max-w-7xl items-center justify-between px-5 py-6 lg:px-8"><button onClick={() => go('home')}><BrandMark /></button><button onClick={() => go('home')} className="font-mono text-xs uppercase tracking-widest text-muted-foreground hover:text-yellow">Back to site</button></header><div className="mx-auto flex min-h-[75vh] max-w-md items-center px-5 py-10"><div className="trading-panel w-full p-7 md:p-10"><p className="font-mono text-xs uppercase tracking-[0.2em] text-yellow">{mode === 'login' ? 'Members only' : 'Start your path'}</p><h1 className="mt-3 text-3xl font-bold tracking-tight">{mode === 'login' ? 'Welcome back.' : 'Create your account.'}</h1><p className="mt-2 text-sm leading-6 text-muted-foreground">{mode === 'login' ? 'Sign in to continue your learning path.' : 'Open access to the Trading Cube Academy.'}</p><form className="mt-8 flex flex-col gap-5" onSubmit={submit}>{mode === 'signup' && <label className="flex flex-col gap-2 text-sm font-medium">Full name<input className="h-12 rounded-md border border-border bg-secondary px-4 outline-none focus:border-yellow" value={name} onChange={(e) => setName(e.target.value)} required /></label>}<label className="flex flex-col gap-2 text-sm font-medium">Email address<input className="h-12 rounded-md border border-border bg-secondary px-4 outline-none focus:border-yellow" type="email" value={email} onChange={(e) => setEmail(e.target.value)} required /></label><label className="flex flex-col gap-2 text-sm font-medium">Password<input className="h-12 rounded-md border border-border bg-secondary px-4 outline-none focus:border-yellow" type="password" minLength={8} value={password} onChange={(e) => setPassword(e.target.value)} required /></label>{message && <p className="border border-yellow/40 bg-yellow/10 p-3 text-sm text-yellow" role="status">{message}</p>}<button className="trading-button-primary flex h-12 items-center justify-center gap-2" type="submit">{mode === 'login' ? 'Enter the academy' : 'Create account'} <ArrowRight data-icon="inline-end" /></button></form><p className="mt-7 text-center text-sm text-muted-foreground">{mode === 'login' ? 'Need an account?' : 'Already a member?'} <button onClick={() => go(mode === 'login' ? 'signup' : 'login')} className="text-yellow hover:underline">{mode === 'login' ? 'Create one' : 'Sign in'}</button></p></div></div></main> }

function Dashboard({ user, signOut, go }: { user: any; signOut: () => void; go: (v: View) => void }) { return <main className="min-h-screen bg-background"><header className="border-b border-border"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-5 lg:px-8"><BrandMark /><div className="flex items-center gap-4"><span className="hidden text-sm text-muted-foreground sm:block">{user?.user_metadata?.full_name ?? user?.email}</span><button onClick={signOut} className="border border-border px-3 py-2 font-mono text-[10px] uppercase tracking-widest text-muted-foreground hover:border-yellow hover:text-yellow">Sign out</button></div></div></header><div className="mx-auto max-w-7xl px-5 py-12 lg:px-8"><p className="font-mono text-xs uppercase tracking-[0.2em] text-yellow">Member dashboard</p><h1 className="mt-3 text-4xl font-bold tracking-tight">Keep building your edge.</h1><p className="mt-2 text-muted-foreground">Your account is live and ready for the learning path.</p><div className="mt-10 grid gap-5 md:grid-cols-3"><div className="trading-panel p-6"><Play className="text-yellow" /><h2 className="mt-5 text-xl font-semibold">The Trader&apos;s Foundation</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">Start with structure, price action, and decision-making.</p><button className="trading-button-primary mt-6 w-full">Continue course</button></div><div className="trading-panel p-6"><BookOpen className="text-yellow" /><h2 className="mt-5 text-xl font-semibold">Course materials</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">Documents and reading notes will appear here as they are published.</p></div><div className="trading-panel p-6"><Settings className="text-yellow" /><h2 className="mt-5 text-xl font-semibold">Account security</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">Signed in with Supabase email authentication.</p></div></div><div className="mt-10"><HelpBlock /></div><button onClick={() => go('admin')} className="mt-8 text-xs text-muted-foreground underline">Admin preview</button></div></main> }

function AdminView({ go }: { go: (v: View) => void }) { const [items, setItems] = useState<any[]>([]); const [supportNumber, setSupportNumber] = useState('447757464428'); const [saved, setSaved] = useState(false); const supabase = createClient(); useEffect(() => { supabase.from('integration_settings').select('provider,label,enabled,public_value').order('provider').then(({ data }) => { if (data) { setItems(data); const whatsapp = data.find((item) => item.provider === 'whatsapp'); if (whatsapp?.public_value) setSupportNumber(whatsapp.public_value) } }) }, [supabase]); const save = async () => { await supabase.from('integration_settings').update({ enabled: true, public_value: supportNumber, updated_at: new Date().toISOString() }).eq('provider', 'whatsapp'); setSaved(true) }; return <main className="min-h-screen bg-background"><header className="border-b border-border"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-5 lg:px-8"><button onClick={() => go('home')}><BrandMark /></button><span className="font-mono text-[10px] uppercase tracking-widest text-yellow">Admin console</span></div></header><div className="mx-auto max-w-5xl px-5 py-12 lg:px-8"><p className="font-mono text-xs uppercase tracking-[0.2em] text-yellow">Integrations</p><h1 className="mt-3 text-4xl font-bold tracking-tight">Connect the academy.</h1><p className="mt-3 max-w-2xl leading-7 text-muted-foreground">Secrets stay in environment variables. This page controls enabled services and safe public configuration such as your WhatsApp support number.</p><div className="mt-10 grid gap-4 md:grid-cols-2">{items.map((item) => <div key={item.provider} className="trading-panel flex items-start justify-between gap-4 p-5"><div><p className="font-semibold">{item.label}</p><p className="mt-1 text-sm text-muted-foreground">{item.provider === 'youtube' ? 'YouTube Data API key remains server-side.' : item.provider === 'blob' ? 'Private course files use Blob storage.' : item.provider === 'whatsapp' ? 'Support links use the public number below.' : 'Optional transactional email provider.'}</p></div><span className={`font-mono text-[10px] uppercase ${item.enabled ? 'text-yellow' : 'text-muted-foreground'}`}>{item.enabled ? 'Enabled' : 'Not configured'}</span></div>)}</div><div className="trading-panel mt-6 p-6"><h2 className="text-xl font-semibold">WhatsApp support</h2><label className="mt-5 flex max-w-md flex-col gap-2 text-sm font-medium">International phone number<input className="h-12 rounded-md border border-border bg-secondary px-4 outline-none focus:border-yellow" value={supportNumber} onChange={(e) => setSupportNumber(e.target.value.replace(/[^0-9]/g, ''))} placeholder="447757464428" /></label><button className="trading-button-primary mt-5" onClick={save}>Save support settings</button>{saved && <p className="mt-3 text-sm text-yellow">Support settings saved.</p>}</div><div className="trading-panel mt-6 p-6"><div className="flex items-center gap-3"><FileUp className="text-yellow" /><div><h2 className="font-semibold">Course materials</h2><p className="text-sm text-muted-foreground">Upload route is protected and stores only the pathname in Supabase.</p></div></div></div></div></main> }

export { HelpBlock }
